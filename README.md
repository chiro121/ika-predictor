# 宝来丸 イカメタル釣果予測 v4

## 起動
```text
pip install pandas numpy
python main.py
```

## v4の主な修正
- 条件ランキングは4条件完全一致のみ。不明・3件未満を除外
- 少数データの100%をそのまま信用しない平滑化
- **信頼度の計算を修正**：旧版のように上位10件を使うだけで100に飽和しない。信頼度は予測の的中確率ではなく、類似データ件数・条件一致度・釣果のばらつきを総合した「データの裏付け度」。
